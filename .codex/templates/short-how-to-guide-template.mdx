---
title: How to {do one specific thing}
description: Complete {specific outcome} in {system/product} with the minimum required steps.
sidebarTitle: {short action}
---

{/*
Authoring rules:
- One guide = one practical outcome.
- Keep it short. Aim for 400–900 words.
- Assume the reader wants to get the job done now.
- No history, vision, or broad conceptual framing unless it directly removes ambiguity.
- Every step must move the user forward.
- Every code block must be copy-pasteable or clearly marked as pseudocode.
- Every placeholder must use angle brackets, for example: `<your-project-id>`
- Include an explicit verification step.
- Include only the troubleshooting items that are likely in practice.
*/}

# How to `{do one specific thing}`

One sentence on the outcome.

For example:  
“Use this guide to `<achieve a concrete result>` in `<product/system>`.”

## Before you start

You need:

- `<required access, role, or permission>`
- `<required account, workspace, wallet, or environment>`
- `<required dependency, SDK, CLI, or API key>`
- `<required starting state>`

Optional but helpful:

- `<optional tool or shortcut>`

## What you will do

In this guide you will:

1. `<step outcome 1>`
2. `<step outcome 2>`
3. `<step outcome 3>`

## Step 1 — `{first action}`

Briefly state what this step does and why it matters.

```bash
# Replace with a real command or request
<command_or_request>
```

If the user needs to edit a file, show the smallest useful example:

```yaml
# Minimal config example
key: <value>
```

Expected result:
- `<what the user should see>`
- `<what should now exist or be enabled>`

## Step 2 — `{next action}`

State the action clearly.

```bash
<command_or_request>
```

If this step includes UI actions, keep them short and literal:
1. Go to `<page or section>`.
2. Select `<control>`.
3. Enter `<value>`.
4. Save the change.

Expected result:
- `<visible confirmation>`
- `<state change>`

## Step 3 — `{complete the flow}`

Show the final action needed to finish the task.

```bash
<command_or_request>
```

Expected result:
- `<final success condition>`

## Verify it worked

Use one or more concrete checks.

```bash
<verification_command>
```

You should see:
- `<verification signal 1>`
- `<verification signal 2>`

If relevant, add a user-facing verification:
- `<where to confirm in the UI>`
- `<what object, status, or record should now appear>`

## Troubleshooting

`<common problem 1>`

Cause: `<most likely cause>`

Fix:

```bash
<fix_command_or_change>
```

`<common problem 2>`

Cause: `<most likely cause>`

Fix:
- `<specific corrective action>`
- `<specific corrective action>`

## Related references

- `<reference page 1>`
- `<reference page 2>`
- `<next guide>`